SELECT
    b.id,
    b.description,
    b.category,
    b.price,
    b.quantity,
    b.collection_time,
    b.status,
    v.name AS vendor_name,
    v.location AS vendor_location
FROM bags b
JOIN vendors v
    ON b.vendor_id = v.id
WHERE b.id = 1;
